pipelines:
  kafka_file:
    input:
      type: kafka
      offset: oldest
      timeout: 60s
      meta:
        partition: "partition_{{ .partition }}"
        topic: "{{ .topic }}"
    output:
      type: file
