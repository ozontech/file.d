pipelines:
  kafka_file:
    input:
      type: kafka
      offset: oldest
      timeout: 5s
      meta:
        partition: "partition_{{ .partition }}"
        topic: "{{ .topic }}"
    output:
      type: file
