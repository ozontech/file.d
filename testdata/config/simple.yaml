pipelines:
  docker:
    input:
      type: file
      persistence_mode: async
      watching_dir: ./testdata/playground/logs
      offsets_file: ./testdata/playground/offsets/docker_offsets.yaml
      reset_offsets: true
    actions:
    - type: k8s
    output:
      type: kafka
      batch_size: 1000
      hosts: infrakafka2.h.o3.ru:9000,infrakafka3.h.o3.ru:9000

actions:
  processor1:
    type: k8s_meta
  processor2:
    type: discard